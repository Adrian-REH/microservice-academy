#Application Configuration
server:
  port: 8099

eureka:
  instance:
    hostname: localhost
    user: root
    password: s3cr3tEureka
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl: 
      defaultZone: http://${eureka.instance.user}:${eureka.instance.password}@${eureka.instance.hostname}:${server.port}/eureka/
